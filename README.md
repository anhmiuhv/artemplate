# artemplate

Template for AR rendering

## How  to compile
* Install yarn or npm if you dont already have them on your computer
* Run these command
```
yarn install
yarn build
```
* All the built javascript files is in the `dist/` folder. Open it on your phone's `WebArOnArCore` or `WebArOnArKit`